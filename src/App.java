import java.awt.Color;
import java.awt.Font;

import javax.swing.BorderFactory;
import javax.swing.ImageIcon;
import javax.swing.JLabel;
import javax.swing.border.Border;

public class App {
    public static void main(String[] args){

        //JPanel = A GUI components that can hold other GUI components

        

        MyFrame myFrame = new MyFrame(); //creates a new JFrame window
        //myFrame.add(new MyLabel());
        /*
         * can be used with default layout of the frame
         * resizes the frame to match the size of the label
         */
        //myFrame.pack();

    }
}